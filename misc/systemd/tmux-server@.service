[Unit]
Description=tmux Server %i
After=default.target graphical-session.target

[Service]
Type=forking
ExecStart=/usr/bin/tmux -L %i start-server \; set -g exit-empty off
ExecStop=/usr/bin/tmux -L %i kill-server
Slice=app-tmux.slice

[Install]
WantedBy=default.target
